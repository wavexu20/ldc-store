# ============================================
# LDC Store 环境变量配置
# ============================================
# 复制此文件为 .env 并填写实际值
# cp .env.example .env

# 数据库由 wrangler.jsonc 中的 Cloudflare D1 `DB` Binding 提供，
# 不需要数据库连接字符串或数据库密码。

# ============================================
# 认证配置 (必填)
# ============================================
# NextAuth 加密密钥 (生成命令: openssl rand -base64 32)
AUTH_SECRET="your-auth-secret-key-here"
AUTH_URL="https://game3dtech.com"

# 信任主机 (Vercel 部署必须设置为 true)
AUTH_TRUST_HOST=true

# 管理员登录密码
ADMIN_PASSWORD="your-secure-admin-password"

# 管理员用户名白名单 (可选)
# 允许通过 Linux DO OAuth 登录后台的用户名列表，逗号分隔
# 留空则只能使用密码登录后台
ADMIN_USERNAMES=""

# ============================================
# Linux DO Credit 支付配置（可选，仅用于历史兼容）
# ============================================
# 在 https://credit.linux.do 创建应用后获取
LDC_CLIENT_ID="your_client_id"
LDC_CLIENT_SECRET="your_client_secret"

# 支付网关地址 (一般无需修改)
LDC_GATEWAY="https://credit.linux.do/epay"

# ============================================
# Game3DTech 自有支付网关（商品购买与余额充值）
# ============================================
PAYMENT_GATEWAY_URL="https://pay.game3dtech.com"
PAYMENT_GATEWAY_APP_ID="game3dtech"
PAYMENT_GATEWAY_API_KEY="your_app_api_key"

# ============================================
# Linux DO OAuth2 登录配置 (用户下单/查单必须)
# ============================================
# 在 https://connect.linux.do 申请应用后获取
LINUXDO_CLIENT_ID="your_linuxdo_client_id"
LINUXDO_CLIENT_SECRET="your_linuxdo_client_secret"

# Google OAuth（可选；配置后登录页自动显示）
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# GitHub OAuth（可选；配置后登录页自动显示）
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Discord OAuth（可选；回调地址：https://game3dtech.com/api/auth/callback/discord）
DISCORD_CLIENT_ID=""
DISCORD_CLIENT_SECRET=""

# Steam OpenID（从 https://steamcommunity.com/dev/apikey 获取）
STEAM_WEB_API_KEY=""

# Cloudflare Email Service 发件地址（域名必须已完成 Email Sending onboarding）
EMAIL_FROM="noreply@game3dtech.com"

# Cloudflare Turnstile（注册人机验证）
NEXT_PUBLIC_TURNSTILE_SITE_KEY=""
TURNSTILE_SECRET_KEY=""

# OAuth2 端点 (一般无需修改，使用默认值即可)
# LINUXDO_AUTHORIZATION_URL="https://connect.linux.do/oauth2/authorize"
# LINUXDO_TOKEN_URL="https://connect.linux.do/oauth2/token"
# LINUXDO_USERINFO_URL="https://connect.linux.do/api/user"

# ============================================
# 网站配置 (可选)
# ============================================
# 网站名称 (显示在 Header 和浏览器标题)
NEXT_PUBLIC_SITE_NAME="LDC Store"

# 网站描述 (用于 SEO)
NEXT_PUBLIC_SITE_DESCRIPTION="基于 Linux DO Credit 的虚拟商品自动发卡平台"

# ============================================
# 订单配置 (可选)
# ============================================
# 订单过期时间 (分钟)，默认 5 分钟
ORDER_EXPIRE_MINUTES=5

# ============================================
# 统计配置 (可选)
# ============================================
# 统计口径时区（用于“今日销售”等报表口径），默认 Asia/Shanghai（UTC+8）
# 建议使用 IANA 时区名，如 Asia/Shanghai、UTC
STATS_TIMEZONE="Asia/Shanghai"

# 退款配置 (可选)
# ============================================
# 退款模式: client (客户端,默认) / proxy (代理) / disabled (禁用)
# client: 通过浏览器表单提交绕过 CORS/CF 限制，无需代理
# proxy: 通过服务端代理调用 LDC API，需配置 LDC_PROXY_URL
# disabled: 禁用退款功能
LDC_REFUND_MODE="client"

# LDC API 代理地址 (仅代理模式时需要)
# 参考: https://github.com/gptkong/gin-flaresolverr-proxy
# LDC_PROXY_URL="https://your-proxy-domain.com/api"
